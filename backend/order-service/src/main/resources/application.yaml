spring:
  application:
    name: order-service
  datasource:
    url: jdbc:mysql://localhost:3306/gula_order_db
    username: root
    password: P@ssW0rd32!
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Jakarta
        dialect: org.hibernate.dialect.MySQLDialect
server:
  port: 8083