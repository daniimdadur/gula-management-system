spring:
  application:
    name: product-service
  datasource:
    url: jdbc:mysql://localhost:3306/gula_product_db
    username: root
    password: P@ssW0rd32!
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Jakarta
        dialect: org.hibernate.dialect.MySQLDialect
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
  api-docs:
    path: /api-docs
server:
  port: 8085
